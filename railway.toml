[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "node_modules/.bin/prisma migrate deploy; node src/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
