[deploy]
startCommand = "npx prisma migrate deploy && node --experimental-strip-types src/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
